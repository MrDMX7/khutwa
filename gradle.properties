org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
org.gradle.caching=true
# On-device builds need android.aapt2FromMavenOverride and a smaller heap;
# those live in ~/.gradle/gradle.properties on the phone, never here — a CI
# runner has no /data/data/com.termux and fails resource processing with it.

# Khutwa v3 — خطة التطوير قبل Google Play

> الحالة عند كتابة الخطة (2026-09-06): v2.1، 45 ملف Kotlin (~7,600 سطر)، محرك دقيق مُثبت (delta = 0)،
> جلسات GPS، مدرّب صوتي، 15 مقالة. عربي فقط ومكتوب داخل الكود. root اختياري ومعزول في `RootBridge.kt`
> (نقطتا استدعاء في `SettingsScreen.kt`). `targetSdk 34`، بلا توقيع release.

## القرار المعماري — نسختان من كود واحد

Gradle **product flavors**، لا فرعان في git:

| | `personal` | `play` |
|---|---|---|
| applicationId | `com.dmx.khutwa` — **يحافظ على بياناتك المثبتة** | `com.elyoxe.khutwa` — هوية العلامة، دائم على Play |
| root | `src/personal/…/RootFeatures.kt` حقيقي + قسم Root في الإعدادات | `src/play/…/RootFeatures.kt` stub — **لا `su` في الكود إطلاقاً** (لا يُفحص ولا يُذكر) |
| البناء | على الهاتف بالـskill الحالي، debug-signed (كما الآن) | GitHub Actions، release-signed بمفتاح في Secrets، minify مفعّل |
| التوزيع | APK يُثبَّت بالـbridge | AAB → Play (اختبار مغلق → إنتاج) |

المشترك كله في `src/main/`. الفرق ملفان + manifest جزئي. أي ميزة جديدة تُكتب مرة واحدة.

### ⚠️ ثوابت لا تُكسر
- **delta = 0**: مجموع الأيام منذ آخر إقلاع = قيمة `TYPE_STEP_COUNTER`. زر التشخيص يبقى ويُشغَّل بعد كل مرحلة.
- الخطوات غير المصنّفة تبقى غير مصنّفة — لا تُدمج في المشي.
- المدرّب يستهدف إيقاع المستخدم +5–10%، لا 180.
- `MapTiles.kt` الملف الوحيد الذي يفتح الشبكة (وHealth Connect يتصل بمزوّد محلي، لا إنترنت). لا إعلانات ولا تتبّع.
- `onUpgrade` يهاجر فعلاً — لا رفع لـ`VERSION` بدون migration. minSdk 26 = لا UPSERT.

## المراحل — كل واحدة تُشحن وتُثبَّت على الفولد قبل التالية

### ✅ P0 — الأساس (منجز 2026-09-06)
- flavors كما فوق · `compileSdk/targetSdk 35` (شرط Play للتطبيقات الجديدة) · AGP 8.7 + Kotlin 2.0 + Compose compiler plugin + BOM حديث
- `infra/actions/build-play.yml`: اختبارات → `bundleRelease` موقّع → artifact. الـkeystore يُولَّد مرة، يُحفظ في GitHub Secrets **وفي S3 المشفّر** (فقدانه = فقدان القدرة على تحديث التطبيق للأبد)
- **تحقق فعلاً:** personal بُني على الهاتف وثُبِّت فوق الحالية بلا فقد بيانات (استمرار العدّ 1,158→1,329)؛ play بُني وتم توقيعه على Actions (R8، targetSdk 35) وثُبِّت بجانبه ويعمل. الـkeystore في `/root/keys` + S3 مشفّر + Actions secrets.
- ملاحظة: الهاتف يبني بـcompileSdk 34 (aapt2 نسخة Termux لا يقرأ موارد 35) وCI بـ35 — عبر خاصية `khutwa.compileSdk` في `~/.gradle/gradle.properties` على الجهاز فقط

### P1 — ثنائية اللغة عربي/إنجليزي (2–3 أيام) — الأكبر
- استخراج ~371 نصاً عربياً من Kotlin إلى `values-ar/strings.xml`، والإنجليزي في `values/` (الافتراضي — لأن Play ومعظم الأجهزة إنجليزية). سكربت بايثون يستخرج ويولّد المفاتيح ويستبدل بـ`stringResource`، ثم مراجعة يدوية للصيغ (`%d`، الجمع، الأرقام العربية)
- المقالات الـ15 (`Knowledge.kt`) → بنية لكل لغة
- اختيار اللغة داخل التطبيق (per-app language على 13+، وبديل للأقدم)
- **التحقق:** بناء وتصوير كل شاشة باللغتين، وRTL/LTR صحيحان

### P2 — نسخ احتياطي بلا root (يوم)
- ملف `.khutwa` (zip: قاعدة البيانات بعد `wal_checkpoint` + الإعدادات JSON) تصدير/استيراد عبر Storage Access Framework — أي مجلد أو Drive
- Android Auto Backup مفعّل بقواعد (يغطي إعادة التثبيت وتغيير الجهاز مجاناً)
- تذكير شهري اختياري · personal يحتفظ بالنسخة الجذرية فوق ذلك
- **التحقق:** تصدير → مسح البيانات → استيراد → delta = 0 وسجل مطابق

### P3 — سلاسل وتحديات + ويدجت (يومان)
- `domain/Streaks.kt`: سلسلة حالية/أطول سلسلة (يوم الهدف)، تسامح يوم راحة قابل للضبط، هدف أسبوعي
- تحديات محلية: «100 ألف في أسبوع»، «30 يوماً متتالية»، «أفضل شهر» — **مصمَّمة ككيان `Challenge` يقبل مصدراً بعيداً لاحقاً** (هذا أساس الـSaaS تحت)
- ويدجت: حلقة تقدم + السلسلة + الهدف، مقاسان؛ إشعار مسائي اختياري «سلسلتك في خطر»
- **التحقق:** اختبارات وحدة للسلاسل (حواف منتصف الليل، أيام بلا بيانات)

### P4 — Health Connect (يومان)
- `connect-client` · **كتابة فقط** (يبقي نموذج أمان البيانات بسيطاً): `StepsRecord` لكل دقيقة مصنّفة + سجل واحد لكل فترة فحص للخطوات غير المصنّفة → **المجموع في HC = مجموعنا**، `ExerciseSessionRecord` + `ExerciseRoute` للجلسات، المسافة والسعرات
- شاشة تبرير الصلاحيات (شرط HC)، مفتاح في الإعدادات **مطفأ افتراضياً**
- **التحقق:** Samsung Health على الفولد يعرض نفس الأرقام

### P5 — جاهزية Play (يوم + 14 يوم اختبار)
- سياسة خصوصية `elyoxe.com/khutwa/privacy` (ar/en) · نموذج أمان البيانات · نص المتجر باللغتين · 8 لقطات بـadb · رسم مميز 1024×500 من `web/_kit`
- حساب Play $25 · 🔴 **الحسابات الشخصية الجديدة تلتزم باختبار مغلق: 12 مختبراً لمدة 14 يوماً متواصلة** قبل السماح بالإنتاج — يُخطَّط له من الآن (أصدقاء/زملاء)

**المجموع:** ~9 أيام عمل + 14 يوم اختبار مغلق. التكلفة: $25 مرة واحدة؛ Actions وHC مجانيان.

## SaaS — التفكير والتوصية

| الخيار | الفكرة | المشتري | التقييم |
|---|---|---|---|
| **A — Khutwa Teams** | تحديات خطوات للمنشآت: لوحة للشركة، كود انضمام، لوحة صدارة، تقارير أسبوعية. التطبيق يبقى مجانياً وخاصاً؛ **يُرفع المجموع اليومي فقط، اختيارياً، لمدة التحدي** | الشركات (عافية الموظفين) — بالمقعد/شهر | **الأقوى.** B2B يطابق تموضع Elyoxe، والسوق موجود (تحدي دبي للياقة 30×30 كل نوفمبر = موسم بيع جاهز). يُبنى على stack الـBLUEPRINT ويصير `saas/_template` الأول |
| B — Khutwa Cloud | مزامنة + لوحة ويب + مشاركة عائلية باشتراك | أفراد | ضعيف: Samsung/Google يقدّمونه مجاناً، والأفراد نادراً ما يدفعون لعدّاد خطوات |
| C — Pro داخل التطبيق | تقرير أسبوعي ذكي (Claude) + رؤى متقدمة، شراء داخل Play | أفراد | عائد صغير لكن بلا بنية تقريباً؛ يصلح **طبقة ثانية** فوق A لاحقاً |

**التوصية:** A، مع تصميم P3 بحيث «التحدي الجماعي» هو نفس كيان التحدي المحلي بمصدر بعيد. الـSaaS نفسه ليس في هذه الخطة — يبدأ بعد نشر v3، بعقد تجريبي مع شركة واحدة قبل أي بناء عام.

## ترتيب التنفيذ المقترح
P0 → P1 → P2 → P3 → P4 → P5. P1 قبل الباقي لأن كل ميزة بعده تُكتب بنصوص مُستخرجة من البداية بدل إعادة العمل.
